[project]
name = "est-mcp"
version = "0.1.0"
description = "MCP server for Hyper Estraier keyword search (Linux/Docker)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.2.4",
    "beautifulsoup4>=4.12",
]
